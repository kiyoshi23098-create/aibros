# Optional — point the frontend at a custom backend endpoint for AI replies.
# Defaults to /api/chat if not set.
# VITE_AI_API_ENDPOINT=/api/chat

# Server-side only (read inside netlify/functions/chat.js).
# Never prefix this with VITE_, or it would be bundled into the frontend.
# ANTHROPIC_API_KEY=your-key-here
